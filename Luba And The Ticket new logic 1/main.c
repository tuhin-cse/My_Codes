#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a[6]={0},b,i=0,c;
    scanf("%d",&b);
    while(b)
    {
      a[i]=b%10;
      i++;
      b=b/10;
    }
    int x,y,x,j,k;
    x=y=z=0;
    for(i=0;i<3;i++)
    {
        for(j=0;j<=i;j++)
        {
            for(k=0;k<=j;k++)
            {
                for()
            }
        }
    }

    return 0;
}
