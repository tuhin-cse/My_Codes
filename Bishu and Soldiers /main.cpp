#include <bits/stdc++.h>

using namespace std;

int binanyS(int a[],int s,int e,int f)
{
    if(arr[s]<=f && arr)
    if(s<e)
    {
        int mid=(s+e)/2;
        if()
    }
}

int main()
{

    return 0;
}
